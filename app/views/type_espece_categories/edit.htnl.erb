<div class="form-container">

  <div class="form-titre">
    <h1>Modifier Type d espece categorie</h1>
  </div>

  <div class="form-formulaire">
    <%= simple_form_for @typeEspeceCategorie do |f| %>
      <%= f.collection_select :biotop_id, Biotop.all, :id, :categorie, prompt: 'Sélectionnez un biotope' %>
      <%= f.input :categorie, placeholder: 'Nom' %>
      <%= f.input :photo, as: :file %>
      <div class="form-input">
        <%= f.button :submit, value: 'Validation' %>
      </div>
    <% end %>
  </div>

</div>
